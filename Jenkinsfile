pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo fun'
		sh 'git clone https://github.com/worksoma/server.git'
            }
        }

        stage('Deploy') {
            steps {
                sh '''
echo "stuff"
exit
EOF
                '''
            }
        }
    }
}

